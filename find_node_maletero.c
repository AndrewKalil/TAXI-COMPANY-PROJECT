#include "lists.h"

/**
 *
 */
unsigned int find_node_maletero(list_t *head)
{
	unsigned int i;
	list_t *index;

	index = head;
	if (head == NULL)
		return (NULL);

	for (i = 0; i < 73 && head; i++)
	{
		if (head == NULL)
			return (NULL);
		index = head->next;
		head = position
	}
	return (position)
}
